#include <stdio.h>

int main() {
    int r1, c1, r2, c2, i, j, k;
    int a[10][10], b[10][10], result[10][10];

    printf("Enter rows and columns for the first matrix: ");
    scanf("%d %d", &r1, &c1);
    printf("Enter rows and columns for the second matrix: ");
    scanf("%d %d", &r2, &c2);

    if (c1 != r2) {
        printf("Error! Column of first matrix must equal row of second matrix.\n");
        return 0;
    }

    // Input elements, perform multiplication, and print result using nested loops
    // ...
    for (i = 0; i < r1; ++i)
        for (j = 0; j < c2; ++j) {
            result[i][j] = 0;
            for (k = 0; k < c1; ++k)
                result[i][j] += a[i][k] * b[k][j];
        }
    // ... (Code for printing matrix)
    return 0;
}

OUTPUT:
Resultant Matrix:
19 22 
43 50 
